# 为 Rillshot 做贡献

感谢你帮助 Rillshot 变得更可靠。缺陷复现、测试用例、文档修正和聚焦的代码变更都很有价值。

## 提交问题

- 先搜索已有 Issue，避免重复。
- 给出 Windows 版本、缩放、显示器布局、输入方式和可最小化复现步骤。
- 上传截图、日志或 JSONL 前，请删除账号、工作内容、本地路径等与问题无关的信息。
- 安全漏洞不要使用普通 Issue，请按 `SECURITY.md` 从仓库的 Security 页面提交。

## 开发流程

1. 从小而单一的变更开始，不在一个 PR 中混合无关重构。
2. 先运行 `node tools/check_project_consistency.mjs` 和 `node tools/check_human_docs_language.mjs`。
3. 在 Windows 上使用 MSVC 构建并运行全部核心测试。Windows/WinUI 改动还须说明实际构建和人工验证范围；无需为本项目额外安装 GCC。
4. 修复缺陷时补充能在修复前失败的回归测试；不将静态检查写成真机验收。
5. 更新用户可见行为时，同步 `CHANGELOG.md` 和当前发行说明；AI 提示词包在仓库外独立维护，不随 PR 提交。

## 代码与风格

- C++ 使用 C++23，警告视为错误；所有输入边界检查枚举、数值范围和非有限浮点数。
- 保持核心算法、Windows 适配器和 WinUI 表现层边界；不在 XAML code-behind 复制拼接算法。
- 不降低覆盖保护、输出原子提交、低置信度拒绝或证据分级。
- 用户文档主体使用中文；标识符、命令和上游固有名称保持原文。

## 贡献授权

Rillshot 同时保留社区 `GPL-3.0-only` 版和独立商业许可路线。为了保证维护者对合并代码拥有足够的重新授权权利，首次提交可版权代码前，维护者会要求贡献者完成单独的贡献者许可协议（CLA）。

在 CLA 未提供或未完成时，PR 可以用于评论和设计讨论，但不会合并其可版权代码。维护者不会把“提交了 PR”默认视为已转让商业再许可权。发现与事实性文档修正可由维护者根据内容判断是否需要 CLA。
