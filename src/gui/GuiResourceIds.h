#pragma once

namespace rillshot::gui {

enum ControlId : int {
    pickRegionId = 1001,
    pickPointId,
    browseOutputId,
    startCaptureId,
    stopCaptureId,
    backendId
};

} // namespace rillshot::gui
