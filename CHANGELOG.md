# 变更记录

## 1.2.0｜开发中

- Portable 包根目录新增 `rillshot-cli.exe`，支持长截图参数、`--version`、稳定退出码和 UTF-8 JSON 结果，供脚本与 Agent 工具调用。
- 命令行参数解析移入平台无关模块；新增必填项、重复项、边界值、默认滚动点和覆盖授权测试。人工滚动提示改写入标准错误，避免污染 JSON 标准输出。
- 设置页切换的原生窗口尺寸更新改为与 XAML 呈现帧同步，不再由后台定时器向 UI 线程投递高优先级任务。页面退出和进入分别采用 Fluent 的 167 ms 与 250 ms 时长，并继续遵守系统动画设置。
- README 以 Windows 长截图功能空缺和云文档内部滚动容器为项目定位，补充 WinUI 3、Agent CLI 与主界面截图，并删除生硬或无助于使用的表述。
- 删除容易被误认为产品入口的源码辅助脚本 `run-portable.cmd`；源码构建统一从 `build-release.cmd` 开始，Portable 用户直接运行包根目录的 `Rillshot.exe`。
- 产品、清单、资源、构建脚本和发布文档统一更新为 1.2.0。

## 1.1.9｜2026-08-16

- 2026-09-01 将首发流程收束为核心测试、真实 WinUI 构建、可选启动冒烟、干净 Stable 工作树与 GitHub Release；删除项目级 SHA256 清单、签名参数、第三方复核状态门禁、源码身份测试和基于文本匹配的静态审计。提示词工程升级为 v4.0.0，并把“效率优先、默认不新增 Harness”写入最高权限规则。
- 2026-09-01 首发第二轮审查：保存对话框限制为 PNG/BMP，包身份探测只接受 Windows 的明确成功状态，设置页纯图标返回按钮补充工具提示；源码身份更新为 `1.1.9-source-ready-reviewed-r2`。
- 公开 README 按“用途—功能—要求—安装/使用—构建—贡献—许可”重写，删除重复的版本真实性和唯一下载来源提示；工程门禁不再强制把固定版本写入仓库首页。提示词工程升级为 v3.3.0，并补充本地仓库、既有 GitHub 历史和日常推送规则。
- 2026-08-31 修复源码 ZIP 直接构建：发布脚本不再让 Git 的预期探测失败触发 `NativeCommandError`；无 `.git` 与尚无 commit 的仓库均可构建 Preview，并使用随包源码身份。Stable 继续要求有 commit 的干净 Git 工作树。
- 源码身份解析从主发布脚本拆为独立模块；Windows CI 新增解压目录、空仓库、干净仓库和脏仓库四类回归测试，工程门禁同步保护入口契约。
- WinUI 紧凑主页面和结果页现在保留错误的具体原因与下一步，而不是只显示错误标题；提示词工程升级为 v3.2.0，明确“原生、克制、任务优先、可信”的设计理念以及 Fluent 优先、Material 3 仅作交叉检查的边界。
- 2026-08-30 首发审查：Portable 根入口由 `cmd → PowerShell -ExecutionPolicy Bypass` 改为无控制台原生 `Rillshot.exe`，启动器与 WinUI 主程序统一走 Authenticode 签名和 RFC 3161 时间戳验证。
- GitHub Actions 固定到完整提交 SHA；Windows 作业在原生适配器测试后实际执行 WinUI Restore、Release Build 和 Portable 打包，不再以静态正则门禁代替前端构建。
- 删除同盘复制后的逐文件 SHA 和同一交付 ZIP 内的源码/提示词树 SHA；Stable Direct 在签名和打包完成后只生成一份覆盖公开制品的 `SHA256SUMS.txt`，并保留 Authenticode、GitHub asset digest 与 WinGet InstallerSha256。摘要用于完整性核对，不冒充发布者身份。
- 工程一致性检查改为版本、项目注册、XAML 事件/资源、测试注册、发布布局和 CI 契约等结构性检查；删除 500 行硬上限及与单次事故绑定的实现快照。
- WinUI 可操作控件显式最小高度统一到 40 epx；无行为的固定页头帮助按钮改为说明图标，并把完整说明关联到 NumberBox 的辅助技术帮助文本。
- AI 提示词工程升级为 v3.1.0：六个职责文件聚焦产品/设计、算法、验证/发布、维护和研究；本地 Visual Studio/Node/CMake 配置与 Windows 真机待验证边界写入固定工作流，并按 Direct、GitHub、Store、镜像的实际信任边界管理 checksum。
- 修复 WinUI 工程的 XAML 头文件映射：只有 `MainWindow.xaml.h` 声明 `DependentUpon=MainWindow.xaml`，辅助头不再重复占用映射；发布脚本会清除旧日志，并在 MSBuild 进程误报成功但日志仍含 XAML/WMC/Build FAILED/非零错误汇总时阻断发布。
- 发布输出目录现在每次构建都重新建立，防止旧 ZIP、MSIX 或依赖清单混入当前调用；MSIX 文件枚举改为先递归列出文件再按扩展名过滤，避免 Windows PowerShell 5.1 对 `-LiteralPath` 与 `-Include` 组合的不一致处理。
- 首轮整体审查后的源码身份为 `1.1.9-source-ready-reviewed`；第二轮审查已升级为 `1.1.9-source-ready-reviewed-r2`。处于 Git 仓库中时，发布元数据另记录实际构建 commit。
- 原生窗口几何不再由低于 idle 且可能迟到的 `DispatcherQueueTimer` 驱动；改用按标称刷新率夹紧到 4–16.667ms 的短生命周期线程池脉冲，并以原子门合并为最多一个待处理 UI 更新。
- 主页面与设置页拆成独立 ScrollViewer，过渡期间分别固定起点/终点视口尺寸；大型设置树不再随每个 `MoveAndResize` 采样完整重排。
- 220ms 透明度、位移与视口中心缩放继续由 compositor 执行；预热扩展到预计最终尺寸，快速反向、减少动画与关闭清理保持。
- 紧凑窗口与设置窗口采用 DPI/工作区自适应策略：设置页优先 1000×760 DIP并受 88%×82% 工作区软上限约束，小屏自动滚动；跨 DPI 显示器返回时保持原有效尺寸。
- 窗口几何测试覆盖 800×560 至 4K 代表性工作区、96/144/192 DPI 和跨 DPI 换算；WinUI 静态门禁同步保护自适应策略。
- 修复首发顺序：先公开 `main` 并完成 Windows/许可/签名门禁，再创建不可移动 tag 并从 tag 构建 Stable，避免最终二进制与对应源码 commit 漂移。
- 商业授权 Issue 模板明确说明内容会公开显示，只收集基本使用场景，并提醒申请者不要填写预算、客户名称或未公开代码。
- 重写公开 README：按“用途—下载—使用—故障处理—隐私—构建—贡献”组织，删除对维护者内部 `docs/` 的链接，并将难以理解的算法审查措辞改为普通用户能判断的结果说明。
- 维护者内部 `docs/` 由 `.gitignore` 排除，工程门禁和 CI 在该目录不存在时仍可运行；提示词工程升级为 v2.8.0，固化公开文档与内部资料的边界。
- 修正首次发布收尾流程：README 的反馈入口与实际 Issue 表单一致，缺陷表单版本示例更新为 1.1.9；源码门禁允许第三方载荷复核在 `PENDING` 与 `COMPLETE` 两个合法阶段运行，但 Stable 构建仍只接受 `COMPLETE`。
- GitHub Release 草稿上传 Portable ZIP、同 tag 对应源码、Stable Direct 的单一 `SHA256SUMS.txt` 和必要的依赖/法律材料；上传后仍以平台 asset digest 核对本地 SHA-256。发布说明保存在仓库外，避免依赖不会上传的内部 `docs/`。
- 重新检查公开中文：Issue 表单、安全政策、README、贡献指南和随包说明改用具体操作，不再用“脱敏”“非机密概要”或“私密报告”等抽象说法；Windows 本机测试说明统一以 MSVC 为正式工具链，GCC/sanitizer 仅作为 GitHub Actions 的补充检查。

## 1.1.8｜2026-08-14

- 修复 `MainWindow.Navigation.Animation.cpp` 中两处未限定的 `CompositionTarget`：该类型属于 `Microsoft::UI::Xaml::Media`，不属于文件已导入的 `Microsoft::UI::Composition`；Windows MSVC 报出的四条 C2653/C3861 因此归并为一个根因。
- 两处合成器获取调用改用完整限定名，避免拆分实现文件后依赖其他翻译单元的 `using namespace`；新增机械门禁，要求调用总数与完整限定调用数一致。
- 用户提供的 Windows 日志证明 NuGet Restore 为 0 warning/0 error，随后 ClCompile 在上述两处停止；1.1.8 仍需在 Windows 发布机重新完成 Release Build、CTest、启动与帧时间验证后才能形成二进制发布证据。
- 产品、源码身份和活动发布文档升级为 1.1.8；提示词工程升级到 v2.5.1，增加 WinRT API 命名空间归属与真实编译证据规则。

## 1.1.7｜2026-08-14

- 首帧后空闲预热设置树与六个合成动画模板，正常导航复用模板并避免同步强制完整布局；轻微缩放改以可见内容中心为轴。
- 页面 `Opacity`/`Translation`/`Scale` 继续由 compositor 以 220ms Fluent 曲线处理；原生窗口保留约 16ms 时间驱动更新预算，不用高刷新率回调触发布局。
- 动画期间的新目标合并为最新意图，当前批次完成后最多执行一次跟随切换；完成、异常、减少动画与关闭路径统一恢复属性和命中状态。
- 主题失效延后到低优先级调度轮次并合并连续选择；INI 偏好写入移到串行后台工作项，旧完成回调不能覆盖新状态，窗口关闭时补写最终快照。
- 导航合成与偏好持久化拆为独立职责文件；WinUI 门禁新增预热、模板复用、中心缩放、最新目标、后台写入和关闭补写契约；提示词工程升级为 v2.5.0。

## 1.1.6｜2026-08-14

- 设置窗口按当前 DPI 与显示器工作区平滑扩展到约 1000×900 DIP，返回主页面时恢复进入设置前的尺寸；最大化、小工作区和负坐标显示器安全降级。
- 页面切换升级为 220ms `Opacity`/`Translation`/`Scale` 合成动画；原生窗口使用固定约 16ms DispatcherQueueTimer 与 quintic smootherstep，不随高刷新率增加布局次数。
- 截图区域与滚动点删除演示默认值，使用“点击选择区域”“点击选择滚动点”和 NumberBox“未选择”占位；开始、滚动点和页头按钮在依赖未满足时禁用。
- 重新框选区域会清除旧滚动点；切换滚动方向不再自动生成或移动点，避免暗中覆盖用户选择。
- 配置摘要区分缺少区域、缺少滚动点和点不在区域内；有效输出路径不再因坐标未完成而误显示为无效。
- 窗口几何测试增加工作区夹紧、负坐标、极小工作区和边界插值，WinUI 门禁同步保护动画更新预算与显式选择规则；提示词工程升级为 v2.4.0。

## 1.1.5｜2026-08-14

- 修复 `Signing.ps1` 中 `$FilePath:` 触发的 PowerShell 解析错误；新增官方 AST 全树语法门禁，并在加载发布模块前执行。
- Linux/Node 工程门禁拒绝普通变量紧邻冒号的同型回归；Windows CI 运行 PowerShell 自身解析器，语法扫描排除构建缓存与第三方生成脚本。
- MSBuild 与 CMake 优先取自 `vswhere` 选择的同一 Visual Studio 安装；配置前验证请求的 CMake 生成器能力并提供明确版本诊断。
- SignTool 改为按 Windows SDK `System.Version` 排序，避免目录字典序选错版本；发布 SHA-256 同时覆盖 NuGet 依赖清单。
- 修复全局快捷键线程在消息队列建立前初始化超时后可能永久 `join()` 的窗口：增加跨阶段原子停止状态和窗口发布二次检查。
- 一键构建失败提示不再把解析等错误泛化为 ExecutionPolicy；产品和活动文档统一为 1.1.5，提示词工程升级为 v2.3.0。

## 1.1.4｜2026-08-14

- 修复 CLI 人工滚动模式在 `std::getline` 遇到 EOF 或读取失败时仍返回成功的问题；现在返回 `manual-input-closed`，停止继续采集无效帧。
- 停止原因不再按“人工驱动”一概视为用户停止：只有明确输入 `q`/`Q` 的 `manual-user-stopped` 是正常取消，人工输入关闭或驱动异常均为非正常 `ScrollRejected`。
- 将 `ManualDriver` 移入平台无关核心目标，输入边界测试增加正常确认、明确停止、EOF 和异常分类回归；测试目标数保持八个。
- 共享界面把“滚轮输入未被目标接受”改为适用于滚轮、键盘和人工模式的“滚动输入未完成”，并补充终端可用性指引。
- 产品、清单、资源、CLI、WinUI、构建脚本和活动发布文档统一为 1.1.4；完整交付继续使用源码/提示词平级的单一外层 ZIP。
- 提示词工程升级为 v2.2.0，固化“只有显式用户意图才能标记正常取消”和流/回调 EOF 必须失败关闭的规则。

## 1.1.3｜2026-08-14

- 捕获会话通过独立输入边界包装器拦截滚轮/键盘驱动抛出的标准与未知异常，转换为 `scroll-driver-exception` 结构化失败，并继续执行最后可靠图像的统一保存流程。
- 滚轮与键盘输入用 `CursorPositionGuard` 在异常和提前返回路径恢复原始鼠标位置；随机延迟改用不抛异常的进程内时钟/线程种子。
- 新增 `input_boundary_test`，平台无关测试目标增至八个；工程门禁覆盖输入异常隔离、鼠标恢复、随机源和 WinUI 工程文件引用。
- 补齐 `SessionInputSupport` 在 WinUI `.vcxproj` 中的编译项，避免 CMake 目标已更新但商业前端链接缺符号。
- 清理 Portable/MSIX 共用界面中的“程序目录日志”硬编码，统一为部署模型无关的应用数据目录表述。
- 完整交付改为单个外层 ZIP：源码目录和 AI 提示词目录平级、互不包含；提示词升级为 v2.1.0，并增加可机械复验的双树哈希。
- 继续使用 GPL-3.0-only 社区许可与独立商业许可；Stable 仍受 Windows 真机、签名和实际二进制第三方通知复核阻断。

## 1.1.2｜2026-08-14

- 修复 MSIX 包身份下应用数据根目录为空的问题：Portable 继续使用程序目录，MSIX 的设置、启动日志与默认截图改用包专属 `LocalState`。
- 接缝粗筛改用有界最优候选堆：保持请求步长并遍历原候选，不跳过非步长对齐接缝，只保留最强 4096 项，避免极端合法高度将线性搜索范围放大为失控内存；精排仍最多 32 项。
- 新增极端高度回归、MSIX/Portable 路径静态契约，并保持七个跨平台测试目标。
- 社区许可由 AGPL-3.0-or-later 调整为 GPL-3.0-only，保留强 copyleft 的分发边界和由相应版权持有人单独提供的商业授权路线。
- 源码、清单、UI、构建脚本、发布文档和 GitHub 教程统一为 1.1.2；AI 提示词工程继续独立交付并升级为 v2.0.0。
- 新增高 DPI 原生重渲染路线：先验证目标应用产生更多物理像素，不把普通插值称为超分；AI 增强在能证明文字零臆造前保持非目标。

## 1.1.1｜2026-08-10

- 设置/主页面切换不再从逐刷新 UI 回调缩放原生窗口；改为稳定 HWND 几何、设置页动画前布局，以及 167ms 直接 Composition `Opacity`/`Translation` scoped batch，降低 240Hz 下布局竞争和帧节奏抖动。
- 完成、异常、关闭和系统关闭动画路径统一清理动画对象、透明度、位移、命中状态、可见性与焦点；静态门禁拒绝逐帧窗口几何回退。
- AI 提示词工程移出 Git 源码包，独立升级为 v1.8.0；源码构建、CI 和一致性检查不依赖该包。
- 新增 `COPYRIGHT.md` 与 `SOURCE_OFFER.md`，设置页显示 AGPL 许可/版权/无担保提示，Portable/MSIX 携带社区许可、权利链、对应源码和第三方通知。
- Stable 直发强制有效 Code Signing 证书、SHA-256、HTTPS RFC 3161 时间戳与签后验证；MSIX Publisher 必须精确匹配证书 Subject。
- Microsoft Store 通道独立为未签名 MSIX 流程，要求 Partner Center Publisher 精确匹配并由 Store 审核后签名；实际二进制第三方许可复核未完成时所有 Stable 通道继续硬停止。
- 整体复核稳定帧、后端回退、接缝预算、输出所有权、取消/关闭与快捷键生命周期；未发现需要在补丁版本中改变拼接阈值的新增根因。

## 1.1.0｜2026-08-10

- 设置页进入/退出改为 220ms 并行透明度与位移动画，窗口尺寸使用五次 smootherstep 同步插值；完成、取消、关闭和“减少动画”共用可清理的终态路径。
- 稳定差异和接缝精排使用两个交错采样相位，降低规则纹理恰好落在固定格点间隙时的漏检风险，同时保持最多 32 个精排候选预算。
- 稳定等待的截止时间覆盖最小等待与捕获耗时；捕获后端每次尝试使用独立结果，失败回退不会泄露半写入帧，近黑失败候选按信息量保留。
- 增加默认 512 MiB 组装图预算和 WIC 可编码上限校验；达到上限时以非正常停止保存最后可靠部分和对照帧。
- 会话日志新增精排采样数与输出预算，CLI 新增 `--max-output-mib`，并补齐相位、截止时间、候选隔离、预算和动画几何回归。
- 提示词工程升级到 v1.7.0，加入动画完成边界、双相位采样、真实截止时间、回退隔离和主动内存上限规则。
- 新增完整的中文 Git/GitHub 首次建仓、CI、tag、Release 和后续 PR 工作流教程。

## 1.0.3｜2026-08-09

- 稳定等待超时的首帧或滚动后帧不再进入拼接器；保留最后可靠结果、对照帧和明确诊断。
- 修复图像差异忽略边界、默认滚动点和候选遍历的整数极值/溢出问题。
- 拼接入口拒绝非法方向枚举、非有限阈值与负数选项，并增加极值与 NaN 回归。
- 修复无输出取消误报“截图已完成”，保留 CaptureController 调用方取消回调，补齐中文状态说明。
- 将提示词工程收入仓库并升级到 v1.6.0，加入双授权、贡献权利链、数值边界和稳定拒绝规则。
- 源码采用 AGPL-3.0-or-later，补齐商业许可路径、贡献/安全/商标/行为治理和第三方通知。
- 加入 CMake core/sanitizer presets、Linux/Windows CI、Dependabot 和 Issue/PR 模板；Stable 二进制强制检查实际载荷许可复核标记。

## 1.0.2｜2026-08-07

- 产品、CMake、CLI、Win32/WinUI/MSIX 清单、发布脚本和活动文档统一为 1.0.2。
- 源码身份改为 `1.0.2-source-reviewed`；后续使用产品版本、语义状态或提交哈希，不再累加内部修订编号。
- 完成窗口坐标安全的第二轮审查：初始窗口、设置动效、DPI 建议矩形和全屏选择覆盖层统一使用宽差值、受检跨度/偏移或饱和渲染坐标。
- 公共 DIP 转像素逻辑加入非有限值和超范围保护；窗口几何测试覆盖完整 32 位坐标差、跨度、偏移、插值和 DPI 缩放。
- 测试辅助函数不再先用 32 位整数相减后取绝对值。
- 修复 `MainWindow.cpp` 中几何函数局部 using 声明越出作用域后导致的 MSVC C3861；调用点统一使用 `geometry::` 限定名。
- DXGI 捕获增加真实纹理尺寸、BGRA 格式、RowPitch、源偏移和地址范围验证，并用作用域守卫释放已获取帧与映射纹理；GDI 捕获补齐位图和选择失败分支清理。
- 重写并扩充 1.0.2 中文 README 素材，修正旧版本身份和三个错误源码路径，区分 WinUI 当前只暴露固定页头与共享会话支持固定页脚，并列出正式 README 尚需制作的界面素材。
- 活动文档收束为构建、发布、依赖、品牌、隐私、提示词、README 素材和验收文档；长期结论保留，已吸收的 0.x 与 1.0.0–1.0.1 重复归档从当前源码包移除。
- 提示词工程更新到 v1.5.1，新增构建根诊断、捕获缓冲区布局和资源释放澄清；manifest 继续声明 `Rillshot 1.0.x` 兼容线。

## 1.0.1｜2026-08-07

- 修复设置窗口几何比较与 smoothstep 插值在极端 32 位坐标下的中间值溢出，并新增窗口几何极值测试；平台无关测试增至 7 个。
- 重写项目 README，以源码、测试和一手资料区分能力、算法边界、场景、隐私、构建要求与待验证项。
- 修正“完整分辨率精排”的过强描述：当前实现为 24×24 上限粗网格和最多 32 个候选的有界二级采样 NCC。
- 提示词工程进入 v1.5.0，增加公开主张证据、许可证表述和 Windows 坐标中间值规则。

## 1.0.0｜2026-08-06

- 产品代码身份迁移为 Rillshot；命名空间、工程、清单、输出、Portable 和文档统一。公开品牌仍待目标市场清查。
- 主图标改为不依赖字母的“对角捕获边界 + 长内容带 + 单一接缝”，补齐 WinUI 多尺寸 PNG、多帧 ICO 与原生窗口图标。
- 建立 `core`、`stitch`、`session`、Windows 适配器、共享 GUI 模型与 WinUI 表现层边界；拆分选择快照、拼接测试和工程门禁。
- 固化双向捕获、四种输入、固定边界、低信息/重复纹理保护和 24+8 有界精排预算。
- Portable 根目录收束为唯一 `Rillshot.cmd` 入口、说明/身份/法律材料及 `app`、`support`；源清单和压缩前门禁拒绝运行数据与开发文件。
- Windows App SDK 改为固定稳定组件闭包，Restore 将关键版本解析警告视为错误，并拒绝聚合包、预发布替代、同组件多版本及未使用的 AI/ML/Widgets/ONNX/DirectML 载荷。
- 发布脚本加入 `preview.N`、包内元数据、逐文件复制/哈希、启动冒烟、签名/时间戳、Stable 法律材料阻断和 SHA-256。
- 修复 Portable 暂存清理、UTF-8/UTF-16 转换、恢复检查点所有权和 Windows SDK 图标局部变量冲突等首发前问题。

1.0.0–1.0.1 与 0.x 中仍有长期价值的规则已压缩进本变更记录和主题文档；AI 维护规则另由独立提示词包保存，被吸收的重复快照不再随当前源码包分发。
