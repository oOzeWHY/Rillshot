# 安全政策

## 支持范围

| 版本 | 安全修复 |
|---|---|
| 最新稳定版 | 支持 |
| 预览版 | 仅用于提前验证，不承诺长期支持 |
| 更早版本 | 不支持 |

## 报告漏洞

优先使用 GitHub 仓库的 Private vulnerability reporting。如果该功能尚未开启，请只创建一个不含利用细节的 Issue，请求维护者提供私密联系渠道。不要在公开 Issue、截图或 JSONL 中暴露敏感数据。

报告建议包含：受影响版本与平台、威胁模型、最小复现、实际/预期结果、已知缓解方式。维护者会先确认收到，再评估影响、修复和披露节奏；在补丁可用前请避免公开利用细节。

Rillshot 会捕获用户主动选择的屏幕区域并注入用户配置的滚动输入，因此权限跨越、未授权捕获、路径覆盖、敏感诊断泄漏和伪造更新/发行件都在安全范围内。
